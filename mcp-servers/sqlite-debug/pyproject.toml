[project]
name = "sqlite-debug-mcp"
version = "0.1.0"
description = "MCP server for debugging SQLite database"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[project.scripts]
sqlite-debug = "sqlite_debug:main"
