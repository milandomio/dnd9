[project]
name = "fmodel-query-mcp"
version = "0.1.0"
description = "MCP server for querying FModel extracted game data"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[project.scripts]
fmodel-query = "fmodel_query:main"
